# Angular with ASP.NET Core (.NET 8)

A demo project using Angular, .NET 8, and ASP.NET Core MVC. The ASP.NET Core
backend is a thin host: it renders a single landing view that boots an Angular
single-page application built by the Angular CLI and served as static files.

> This project was migrated from ASP.NET MVC 5 / .NET Framework 4.8 to
> ASP.NET Core on .NET 8. `System.Web`, `System.Web.Optimization` bundling,
> `Global.asax` and `Web.config` are gone; their responsibilities now live in
> `Program.cs` (routing, static files, MVC) and the Angular CLI build.

## Features

- Angular (XLTS-compatible tooling) front-end - installed using npm
- .NET 8 (ASP.NET Core MVC)
- Angular CLI build output served as static files (replaces `System.Web.Optimization` bundling)
- Playwright end-to-end tests with video evidence

## Prerequisites

- [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)
- [Node.js](https://nodejs.org/) 22.12+ (Angular 21 CLI requirement) and npm 9+

## Getting Started

- Clone repository: `git clone https://github.com/xlts-dev/angularjs-asp-net48-mvc5.git`.
- Switch to the project's directory: `cd angularjs-asp-net48-mvc5`.
- Install npm packages: `npm install`.
- Build the Angular front-end (outputs to `Content/app/browser`): `npm run build`.
- Run the ASP.NET Core app:

  ```bash
  dotnet run --project angularjs-asp-net48-mvc5.csproj --urls http://localhost:5000
  ```

- Open http://localhost:5000/ in your browser. The landing page renders the
  Angular component and directive.

## Project layout

| Path | Purpose |
|------|---------|
| `Program.cs` | Minimal hosting: MVC controllers + views, static files, routing |
| `Controllers/LandingController.cs` | Serves the landing view at `/` |
| `Views/Landing/Index.cshtml` | HTML shell that boots the Angular SPA |
| `src/` | Angular application source |
| `Content/app/browser/` | Angular CLI build output (git-ignored, generated by `npm run build`) |
| `e2e/` | Playwright tests and page objects |
| `.github/workflows/ci.yml` | CI: build + E2E + upload report/videos |

## End-to-end tests

The Playwright suite starts the app automatically (via the `webServer` block in
`playwright.config.js`), so the run is self-contained:

```bash
npm ci
npm run e2e
```

Artifacts:

- `playwright-report/` — HTML report with an embedded video per test.
- `test-results/` — raw `.webm` recordings.

CI regenerates and uploads both on every push/PR.
