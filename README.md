# Angular with ASP.NET Core
A demo project using Angular and ASP.NET Core 8.0 MVC.

## Features

- Angular (modern) frontend
- ASP.NET Core 8.0 MVC backend
- Static file serving for Angular build output
- Cross-platform support (Windows, macOS, Linux)

## Prerequisites

- [.NET 8 SDK](https://dotnet.microsoft.com/en-us/download/dotnet/8.0)
- [Node.js](https://nodejs.org/) (v18 or later)
- npm (v9 or later)

## Getting Started

1. Clone the repository:
   ```bash
   git clone https://github.com/COG-GTM/angularjs-asp-net48-mvc5.git
   cd angularjs-asp-net48-mvc5
   ```

2. Install npm packages and build the Angular frontend:
   ```bash
   npm install
   npm run build
   ```

3. Run the ASP.NET Core application:
   ```bash
   dotnet run
   ```

4. Open your browser and navigate to the URL shown in the console output (e.g., `http://localhost:5000`).
