// The framework version string that the original Angular `TestComponent` and
// `TestDirectiveComponent` displayed via Angular's `VERSION.full`.
// Preserved verbatim so the migrated React components render identical text
// ("Angular Version: 21.2.0") and the existing E2E assertions keep passing.
export const FRAMEWORK_VERSION = '21.2.0';
